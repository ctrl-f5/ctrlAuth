<?php
/** @var $this \Ctrl\View\PhpView */
echo $this->pageTitle($role->getName(), 'edit role');

$form->setLabel('Please provide your credentials');
echo $this->ctrlForm()->createForm(
    $form,
    array(
        $this->ctrlButton('submit', array('value' => 'save'), 'primary'),
        $this->ctrlButton('link', array(
            'value' => 'cancel',
            'url' => $this->url('ctrl_auth', array(
                'controller' => 'role',
            )),
        )),
    )
);